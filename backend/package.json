{
  "name": "multistore-backend",
  "version": "1.0.0",
  "description": "Backend API for Multi-Store Query Mediator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed:all": "npm run seed:mongo && npm run seed:postgres && npm run seed:redis",
    "seed:mongo": "node ../scripts/seed-mongodb.js",
    "seed:postgres": "node ../scripts/seed-postgres.js",
    "seed:redis": "node ../scripts/seed-redis.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "pg": "^8.11.0",
    "redis": "^4.6.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
